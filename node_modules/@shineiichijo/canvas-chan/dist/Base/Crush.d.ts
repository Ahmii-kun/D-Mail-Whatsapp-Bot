/// <reference types="node" />
export declare class Crush {
    private image;
    private path;
    private utils;
    /**
     * Constructs an instance of the Crush class
     * @param {string | Buffer} image Image of the crush
     */
    constructor(image: string | Buffer);
    /**
     * Builds the image
     * @returns {Buffer}
     */
    build: () => Promise<Buffer>;
}

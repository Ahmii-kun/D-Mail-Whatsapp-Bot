/// <reference types="node" />
export declare class Danger {
  private text;
  private paths;
  private utils;
  /**
   * Constructs an instance of the Danger class
   * @param {string} text Text that should be written
   */
  constructor(text: string);
  /**
   * Builds the image
   * @returns {Buffer}
   */
  build: () => Promise<Buffer>;
}

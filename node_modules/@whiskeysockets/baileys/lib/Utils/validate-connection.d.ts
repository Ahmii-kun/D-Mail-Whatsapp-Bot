import { proto } from '@whiskeysockets/baileys/WAProto';
import type { AuthenticationCreds, SignalCreds, SocketConfig } from '@whiskeysockets/baileys/lib/Types';
import { BinaryNode } from '@whiskeysockets/baileys/lib/WABinary';
export declare const generateMobileNode: (config: SocketConfig) => proto.IClientPayload;
export declare const generateLoginNode: (userJid: string, config: SocketConfig) => proto.IClientPayload;
export declare const generateRegistrationNode: ({ registrationId, signedPreKey, signedIdentityKey }: SignalCreds, config: SocketConfig) => proto.ClientPayload;
export declare const configureSuccessfulPairing: (stanza: BinaryNode, { advSecretKey, signedIdentityKey, signalIdentities }: Pick<AuthenticationCreds, 'advSecretKey' | 'signedIdentityKey' | 'signalIdentities'>) => {
    creds: Partial<AuthenticationCreds>;
    reply: BinaryNode;
};
export declare const encodeSignedDeviceIdentity: (account: proto.IADVSignedDeviceIdentity, includeSignatureKey: boolean) => Uint8Array;
